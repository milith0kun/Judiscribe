# === Core Framework ===
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# === Database ===
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# === Task Queue ===
celery>=5.4.0
redis>=5.0.0

# === AI Services ===
deepgram-sdk>=3.7.0
anthropic>=0.40.0

# === Auth ===
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.0

# === HTTP/WebSocket ===
httpx>=0.27.0
websockets>=12.0

# === Document Generation (Sprint 4) ===
python-docx>=1.1.0

# === REMOVED (not used) ===
# weasyprint>=62.0        # PDF generation - not implemented
# python-Levenshtein>=0.25.0  # String similarity - never imported
# jellyfish>=1.0.0        # Phonetic matching - never imported
# jiwer>=3.0.0            # WER metrics - never imported
# pydub>=0.25.0           # Audio processing - never imported
# ffmpeg-python>=0.2.0    # FFmpeg bindings - never imported
# noisereduce>=3.0.0      # Noise reduction - never imported
